# Performance Tuning

Collect guidance for keeping the simulator + planners real-time.

- GPU requirements for `local_sensing` CUDA nodes.
- Recommended launch args (e.g., `use_sim_time`, QoS overrides).
- Profiling tools or metrics to watch (Hz, latency, CPU/GPU usage).
