# Visualization

Document RViz/foxglove setups for monitoring planner state.

## Checklist
- RViz config path (`files/rviz/*.rviz`).
- TF tree expectations (`world` → `body` → `camera`).
- Helpful displays: point clouds, trajectories, PolyTraj commands.

Add screenshots or GIFs under `files/` when available.
