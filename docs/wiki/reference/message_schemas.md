# Message Schemas

Summarize custom messages (e.g., `quadrotor_msgs/Bspline`, `quadrotor_msgs/PositionCommand`).

| Message | Source | Notes |
|---------|--------|-------|
| `quadrotor_msgs/PositionCommand` | `src/uav_simulator/Utils/quadrotor_msgs/msg/PositionCommand.msg` | Consumed by `traj_server` and controllers |

Link back to `.msg` files rather than duplicating the full schema.
