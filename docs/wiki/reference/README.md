# Reference Section

Use this for authoritative tables and schemas that other sections can link to.

## Pages

- `message_schemas.md` – custom message definitions and topic contracts
- `parameter_tables.md` – key parameters per package (YAML source of truth + explanation)
- `cli_reference.md` – frequently used CLI commands (ros2, colcon, rviz)
- `mppi_controller.md` – MPPI controller variants, parameters, and usage guide

Keep entries synced with source headers and YAML configs.
