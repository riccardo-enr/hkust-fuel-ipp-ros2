# CLI Reference

List frequently used commands with short explanations.

| Command | Purpose |
|---------|---------|
| `ros2 launch plan_manage traj_server.launch.py world:=office` | Bring up trajectory server with office world |
| `ros2 topic echo /traj_server/command` | Inspect outgoing commands |
| `ros2 run tf2_tools view_frames` | Visualize TF tree |

Expand as new workflows stabilize.
